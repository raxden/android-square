apply plugin: 'com.android.library'

group = "com.raxdenstudios.square.interceptor.commons"
version = VERSION_NAME

android {
    compileSdkVersion rootProject.ext.compileSdkVersion
    buildToolsVersion rootProject.ext.buildToolsVersion

    defaultConfig {
        minSdkVersion rootProject.ext.minSdkVersion
        targetSdkVersion rootProject.ext.targetSdkVersion

        consumerProguardFiles 'proguard-rules.pro'
    }
}

dependencies {
    implementation fileTree(include: '*.jar', dir: 'libs')
    implementation project(':square')

    implementation deps.square_commons.implementation
    api deps.square_commons.api
    annotationProcessor deps.square_commons.annotationProcessor
    compileOnly deps.square_commons.compileOnly
    testImplementation deps.square_commons.testImplementation
    androidTestImplementation deps.square_commons.androidTestImplementation
}

apply from: 'build_javadoc.gradle'