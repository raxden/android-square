apply plugin: 'com.android.library'

group = "com.raxdenstudios.square.interceptor.cast"
version = VERSION_NAME

android {
    compileSdkVersion rootProject.ext.compileSdkVersion
    buildToolsVersion rootProject.ext.buildToolsVersion

    defaultConfig {
        minSdkVersion rootProject.ext.minSdkVersion
        targetSdkVersion rootProject.ext.targetSdkVersion

        consumerProguardFiles 'proguard-rules.pro'
    }
}

dependencies {
    implementation fileTree(include: '*.jar', dir: 'libs')
    implementation project(':square')

    implementation deps.square_cast.implementation
    api deps.square_cast.api
    annotationProcessor deps.square_cast.annotationProcessor
    compileOnly deps.square_cast.compileOnly
    testImplementation deps.square_cast.testImplementation
    androidTestImplementation deps.square_cast.androidTestImplementation
}

apply from: 'build_javadoc.gradle'