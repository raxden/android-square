apply plugin: 'com.android.library'

group = "com.raxdenstudios.square.interceptor.rater"
version = VERSION_NAME

android {
    compileSdkVersion rootProject.ext.compileSdkVersion
    buildToolsVersion rootProject.ext.buildToolsVersion

    defaultConfig {
        minSdkVersion rootProject.ext.minSdkVersion
        targetSdkVersion rootProject.ext.targetSdkVersion

        consumerProguardFiles 'proguard-rules.pro'
    }
}

dependencies {
    implementation fileTree(include: '*.jar', dir: 'libs')
    implementation project(':square')

    implementation deps.square_rater.implementation
    api deps.square_rater.api
    annotationProcessor deps.square_rater.annotationProcessor
    compileOnly deps.square_rater.compileOnly
    testImplementation deps.square_rater.testImplementation
    androidTestImplementation deps.square_rater.androidTestImplementation
}

apply from: 'build_javadoc.gradle'