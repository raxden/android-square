apply plugin: 'com.android.library'

group = "com.raxdenstudios.square.interceptor.reactive"
version = VERSION_NAME

android {
    compileSdkVersion rootProject.ext.compileSdkVersion
    buildToolsVersion rootProject.ext.buildToolsVersion

    defaultConfig {
        minSdkVersion rootProject.ext.minSdkVersion
        targetSdkVersion rootProject.ext.targetSdkVersion

        consumerProguardFiles 'proguard-rules.pro'
    }
}

dependencies {
    implementation fileTree(include: '*.jar', dir: 'libs')
    implementation project(':square')

    implementation deps.square_reactive.implementation
    api deps.square_reactive.api
    annotationProcessor deps.square_reactive.annotationProcessor
    compileOnly deps.square_reactive.compileOnly
    testImplementation deps.square_reactive.testImplementation
    androidTestImplementation deps.square_reactive.androidTestImplementation
}

apply from: 'build_javadoc.gradle'